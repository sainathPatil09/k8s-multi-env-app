apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config
immutable: false # will not be update from commands
data:
  mongodb.conf: |
    storage:
      dbPath: /data/configdb
    replication:
      replSetName: "rs0"